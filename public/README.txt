Letakkan file logo Anda di sini dengan nama: logo-tefa.png
(disarankan ukuran persegi, minimal 192x192px, background transparan/PNG)

Jika file ini belum ada, aplikasi tetap berjalan normal —
elemen <img> akan otomatis disembunyikan (onerror) dan
nama toko tetap tampil sebagai teks.
